import React from "react";

const Footer = () => {
  return (
    <footer className="footer">
      <p>&copy; {new Date().getFullYear()} Zomato Admin Panel</p>
      {/* Additional footer content */}
    </footer>
  );
};

export default Footer;
